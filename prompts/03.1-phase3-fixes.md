# Phase 3 — Fix Round (architect review 2026-07-14)

Phase 3 passed review conditionally. The flows work end-to-end in a production
build (verified by the architect on :3100). Four findings below must be fixed,
in this order. Do not touch anything else. `prompts/`, `skills/`,
`scripts/checks/` remain READ-ONLY.

## Context you must know first

- The architect found and already fixed a storage RLS bug (migration 0004 —
  committed). `npm run check:rls` now runs 9 tests including storage policies
  (TEST 9). Run it before and after your changes; all 9 must pass.
- Your Phase 3 delivery was committed by the architect (808c4d7) because it was
  left uncommitted. This round: you commit your own work, per finding, with
  clear messages.

## Finding 1 — BUG (high): publisher-uploaded videos never play anywhere

The wizard stores `cats.video_path` WITH its file extension
(`{catId}/{uuid}-video.mp4` — components/cats/UploadStep4.tsx). Both playback
components append ANOTHER extension pair:

- components/cats/CatCard.tsx (~line 155): `src={...${cat.video_path}.webm}` / `.mp4`
- components/cats/GalleryVideoPlayer.tsx (~lines 87-88): same pattern

So publisher uploads request `...-video.mp4.webm` → 404 → the video silently
never plays, not in the catalog and not on the detail page. Only the seeded
Phase 2.5 clips (extension-less base path with a pre-optimized .webm/.mp4 pair)
work.

Required behavior (ARCHITECTURE §11 video policy v1):

- `video_path` WITH extension ⇒ RAW publisher upload ⇒ plays ONLY on the
  detail page (GalleryVideoPlayer), tap-to-play, a single `<source>` with the
  exact stored path (no extension appending).
- `video_path` WITHOUT extension ⇒ pre-optimized pair ⇒ current behavior
  (catalog autoplay via PlaybackDirector + detail page, both sources).
- CatCard must NOT render the video element or the video-affordance badge for
  raw (extension-bearing) paths — poster only.

Put the "has extension?" helper in one shared util, not duplicated.
Acceptance: upload a short mp4 through the wizard on a PRODUCTION build; it
plays on tap in the detail page; the catalog card shows the poster with no
video badge; seeded cards still autoplay on hover.

## Finding 2 — a11y: wizard radio groups are one giant keyboard group

All radio inputs render with `name=""` and `value="on"` (questionnaire step 2,
and any other radio groups). Native arrow-key navigation treats every radio on
the page as ONE group. Give each question's radios a unique `name` (the field
name) and real values; keep RHF state behavior identical. Acceptance: arrow
keys move only within one question's options; `npm run gate` clean.

## Finding 3 — UX: HEIC failure shows "[object Object]"

heic2any rejects with a plain object, and UploadStep4's catch does
`err instanceof Error ? err.message : String(err)`. Map heic2any failures to
the existing Hebrew error string for unreadable images (add a ui.json key if
missing). Acceptance: selecting a corrupt .heic shows a readable Hebrew
message; existing previews are untouched.

## Finding 4 — eslint warnings (3) in CatUploadWizard

`react-hooks/incompatible-library` on `watch()` calls inside `goNext`
(components/cats/CatUploadWizard.tsx ~129-130). Use `getValues()` for
imperative reads instead of `watch()` there. Acceptance: `npm run lint`
reports 0 errors AND 0 warnings.

## Finding 5 — Hebrew grammatical gender: female cats are addressed as male

Client-reported. The request page for נלה (female) says "וכיצד תעניקו **לו**
בית חם". Only the SEO title adapts today (`detailTitleMale/Female/Unknown` in
ui.json:156-158, used in app/cats/[id]/page.tsx:42-46). Everything else is
hardcoded masculine.

Mechanism (extend the existing pattern, do not invent a new one):

- Add a helper in lib/strings.ts: `gendered(section, key, sex)` that resolves
  `<key>Male` / `<key>Female` / `<key>Unknown` from ui.json; if the Unknown
  variant is missing fall back to the Male form. Replace the if/else chain in
  app/cats/[id]/page.tsx with it.
- Convert these ui.json keys to the suffixed-triple form and update all
  call sites that render a SPECIFIC cat (detail page chips/labels, catalog
  card if it shows these, request page):
  - `catalog.goodWithLabel`, `goodWithCats`, `goodWithDogs`, `goodWithNeither`
    (מסתדר → מסתדרת)
  - `catalog.neuteredTrue/neuteredFalse` — vet Hebrew: male "מסורס",
    female "מעוקרת"
  - `catalog.vaccinesNone/vaccinesOne/vaccinesMultiple`,
    `catalog.healthFull/healthPartial/healthNone` (מחוסן → מחוסנת)
  - `requests.explainWhyAdopt` (תעניקו לו → תעניקו לה)
- IMPORTANT scoping: FILTER labels in the drawer stay generic masculine —
  they describe search criteria, not a cat. If a key is shared between the
  filter drawer and the detail page, split it into two keys.
- Publish wizard (`publish.description` "ספרו עליו קצת",
  `publish.catDescriptionPlaceholder`): adapt by the sex selected in step 1
  (RHF `getValues('sex')`); before selection default to the male form.
- Unknown sex: use the "/ת" slash form where it reads naturally (precedent:
  detailTitleUnknown), otherwise the male form.

Acceptance: on נלה's pages every string reads feminine (chips, labels, request
hint); a male cat is unchanged; a cat with unknown sex shows slash forms;
`npm run gate` clean. List every changed key in the report.

## Process requirements (non-negotiable, checked mechanically)

1. Commit after each finding (4 commits minimum), conventional messages.
2. Final report MUST include: raw `npm run gate` output, raw
   `npm run check:rls` output (9/9), and the test credentials you used.
3. Verify Finding 1 against a PRODUCTION build (`npm run build` +
   `npx next start`), not dev. Static screenshots are not proof of playback —
   report the network request for the video file returning 200.
4. Local review accounts exist: arch-review-pub@example.com /
   arch-review-adopter@example.com (password: Review!2026). Draft cat
   1879afab-3c93-42e7-98ff-8fb07c75c3a3 belongs to the publisher account.
