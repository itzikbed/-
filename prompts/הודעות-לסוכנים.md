# ההודעות לסוכנים — מצב מקומי (ללא חשבונות ענן)

לפני הודעה 1 ודא שמותקן על המחשב: **Node.js LTS**, **Docker Desktop** (חובה ל-Supabase
מקומי — פתוח ורץ), **Git**.

**ההכנה היחידה שלך:** צור תיקייה ריקה (למשל `C:\Users\itzik\projects\cat-adoption`)
ופתח אותה כ-Workspace באנטי-גרביטי. את כל השאר — העתקת המסמכים, הסקילים והקמת
הפרויקט — הסוכן של הודעה 1 עושה בעצמו.

**כלל: שיחת סוכן חדשה לכל הודעה. מדביקים הודעה → מחכים לסיכום בעברית → חוזרים לקלוד
לביקורת → רק אז ההודעה הבאה.**

---

## הודעה 1 — שלב 1: תשתית (שלד, עיצוב בסיס, DB מקומי, הרשמה/התחברות)

```
You are the lead agent for this project. The workspace is currently empty — you bootstrap it.

STEP 0 — bootstrap the project folder (before anything else):
- Copy the project documents from C:\Users\itzik\.gemini\antigravity\project-docs\ into this workspace as follows:
  - ARCHITECTURE.md and DESIGN.md → workspace root
  - supabase\migrations\0001_init.sql → supabase/migrations/
  - prompts\ folder → prompts/
- Copy the skills folder C:\Users\itzik\.gemini\antigravity\skills\ → skills/ (all 8 skill folders).
- Verify every file copied intact (compare file counts), then `git init` and make an initial commit of the docs.

THEN:
1. Read prompts/00-orchestrator.md in full — these are your standing rules for the entire build.
2. Read ARCHITECTURE.md and DESIGN.md in full.
3. The skill files live in the skills/ folder — read each skill when the orchestrator's skill map says it applies to your current task.
4. Execute prompts/01-foundation.md, including its parallel tracks (use sub-agents for tracks marked ∥, respecting file-ownership boundaries). Note: step 2 of that prompt (copying docs into the repo) is already done by your STEP 0 — skip it. When the prompt says to create the Next.js app, scaffold it INTO this workspace root (create-next-app in a temp subfolder and merge if needed — the docs you copied must remain at the root).

LOCAL MODE — overrides for this build (no cloud accounts exist yet):
- Do NOT create a cloud Supabase project. Use Supabase local development instead: `npx supabase init` then `npx supabase start` (requires Docker Desktop running — if Docker is missing or not running, STOP and tell me in Hebrew exactly what to install/start, then wait).
- Put the local URL and keys that `supabase start` prints into `.env.local` (and keep `.env.local.example` with placeholder values as the prompt requires).
- Apply supabase/migrations/0001_init.sql locally (`npx supabase db reset` applies migrations) and generate types from the LOCAL db (`npx supabase gen types typescript --local`).
- Everything else in the prompt applies unchanged, including all exit criteria.

When you finish: run the full exit criteria, then write your final report IN HEBREW: מה נבנה (רשימת מסכים/קבצים עיקריים), אילו בדיקות עברו ואילו נכשלו, ומה דורש את תשומת ליבי. Include screenshots of /dev/ui and the signup/login pages.
```

---

## הודעה 2 — שלב 2: האתר הציבורי (דף בית, קטלוג+סינון, דף חתול)

```
You are the lead agent continuing an existing build.

1. Read prompts/00-orchestrator.md in full — standing rules.
2. Read ARCHITECTURE.md end-to-end (it reflects what was already built) and DESIGN.md in full — §6a (hero media) is mandatory in this phase.
3. Read the skills listed at the top of the phase prompt from the skills/ folder.
4. Make sure local Supabase is running (`npx supabase start` if not), then execute prompts/02-catalog-landing.md, including its parallel tracks via sub-agents.

Reminder: this phase carries the "alive" design requirement — a flat colored hero, or a landing page without real media, means the phase is NOT done.

When you finish: run the full exit criteria, then write your final report IN HEBREW: מה נבנה, אילו בדיקות עברו/נכשלו, ומה דורש את תשומת ליבי. Include desktop (1280px) + mobile (390px) screenshots of the landing page, the catalog with active filters, and one cat detail page.
```

---

## הודעה 3 — שלב 3: טפסים וזרימות (שאלון מאמץ, מסירת חתול+תמונות, בקשות אימוץ)

```
You are the lead agent continuing an existing build.

1. Read prompts/00-orchestrator.md in full — standing rules.
2. Read ARCHITECTURE.md end-to-end and DESIGN.md in full.
3. Read the skills listed at the top of the phase prompt from the skills/ folder.
4. Make sure local Supabase is running, then execute prompts/03-forms-flows.md, including its parallel tracks via sub-agents.

Testing note (local): create test users through the app's own signup; to make one of them an approved publisher or admin, update the profiles row directly in the local db (`npx supabase db ...` or Studio at the local Studio URL) — document in your report which test users exist and their passwords.

When you finish: run the full exit criteria, then write your final report IN HEBREW: מה נבנה, אילו בדיקות עברו/נכשלו, אילו משתמשי בדיקה קיימים, ומה דורש את תשומת ליבי. Include mobile+desktop screenshots of each wizard step of both wizards and of the request flow.
```

---

## הודעה 4 — שלב 4: פאנל אדמין + מיילים (בלי חשבון Resend)

```
You are the lead agent continuing an existing build.

1. Read prompts/00-orchestrator.md in full — standing rules.
2. Read ARCHITECTURE.md end-to-end and DESIGN.md in full.
3. Read the skills listed at the top of the phase prompt from the skills/ folder.
4. Make sure local Supabase is running, then execute prompts/04-admin-emails.md, including its parallel tracks via sub-agents.

LOCAL MODE — email overrides (no Resend account exists yet):
- Build all six templates and the send wrapper exactly as specified, BUT the wrapper must detect a missing RESEND_API_KEY and then: log the send in the console AND write the rendered HTML to a git-ignored `.local-emails/` folder (filename = template + timestamp) instead of sending.
- Replace the "send to real Gmail" tests with: `npx react-email dev` preview — screenshot all six templates at desktop and mobile widths and verify the skill's checklist visually (RTL alignment, phone numbers not scrambled, button rendering).
- Add to ARCHITECTURE.md §10: real-send verification (Gmail/Outlook) is deferred until a Resend account + verified domain exist.
- Everything else, including the full admin→email integration loop test (emails land in `.local-emails/`), applies unchanged.

When you finish: run the full exit criteria (adapted as above), then write your final report IN HEBREW: מה נבנה, תוצאות בדיקת המרוץ של שני אדמינים, אילו מיילים נוצרו בלולאת האינטגרציה, ומה דורש את תשומת ליבי. Include screenshots of all three admin queues and all six email templates.
```

---

## הודעה 5 — שלב 5: ליטוש ו-QA (בלי דיפלוי)

```
You are the lead agent continuing an existing build.

1. Read prompts/00-orchestrator.md in full — standing rules.
2. Read ARCHITECTURE.md end-to-end and DESIGN.md in full.
3. Read the skills listed at the top of the phase prompt from the skills/ folder.
4. Make sure local Supabase is running, then execute prompts/05-polish-qa.md, including its parallel tracks via sub-agents.

LOCAL MODE — launch overrides (no cloud accounts yet):
- Skip everything Vercel/domain/DNS/SPF/DKIM related. Instead: `npm run build` + `npm run start` and run the QA + Lighthouse audits against that local production server (Lighthouse ≥ 90 on landing + catalog, mobile).
- Create `docs/launch-checklist.md` (in Hebrew) listing every deferred cloud step in order: creating the cloud Supabase project + pushing migrations + storage bucket, moving seed/admin users, Resend account + domain verification + real email tests, Vercel deploy + env vars + auth redirect URLs, domain purchase. This is the handover document for going live later.
- The admin guide (docs/admin-guide.md) and screenshot folder are still required.

When you finish: run the full final gate (adapted as above), then write your final report IN HEBREW: תוצאות ה-QA בכל ארבעת התפקידים, ציוני Lighthouse, ממצאי ביקורת העיצוב ומה תוקן, ומה נשאר פתוח ב-launch-checklist. Include the full screenshot folder.
```

---

## הודעות עזר (למקרה הצורך)

**אם הסוכן עצר באמצע / נתקע:**
```
Continue exactly where you stopped. Re-read prompts/00-orchestrator.md and the current phase prompt if you lost context. Do not restart completed work.
```

**אם הסוכן סיים בלי להריץ את הבדיקות:**
```
You did not run the exit criteria. Run them now (tsc, lint, build, RTL grep, the phase's specific checks) and report the results in Hebrew.
```

**אחרי ביקורת של קלוד (הדבק את רשימת התיקונים מתחת לשורה הזו):**
```
The project architect reviewed your work and found the following issues. Fix ALL of them, run the exit criteria again, and report in Hebrew what was fixed and how you verified each fix:

<כאן מדביקים את הממצאים>
```
